import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="홈"
      description="내가 속한 모든 방의 글을 한데 모아 보여준다. 글마다 어느 방에서 온 것인지 배지가 붙고, 방별 필터를 걸 수 있다."
      milestone="M3 · 피드"
    />
  );
}
